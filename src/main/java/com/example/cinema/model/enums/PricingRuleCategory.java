package com.example.cinema.model.enums;

public enum PricingRuleCategory {
    BASE,       // Giá cơ bản (Thường chỉ có 1 Rule gốc)
    SURCHARGE,  // Phụ thu (Thêm tiền)
    DISCOUNT,   // Giảm giá (Trừ bớt)
    TAX         // Thuế
}
